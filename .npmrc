# Fail `npm install` outright on an unsupported Node version rather than
# printing an EBADENGINE warning and letting the build die later inside Vite,
# where the real cause is much harder to read. See "engines" in package.json.
engine-strict=true
